## Como rodar (Back-end)

1. Vá para `src/main/resources/`.
2. Renomeie `application.properties.example` to `application.properties`.
3. Configure suas credenciais do MySQL no arquivo.
4. Rode o projeto com: `./mvnw quarkus:dev`
